## 修复记录 (2026-02-24)

### ✅ 已完成

**1. 修复前后端路由不匹配问题**
- 移除了临时测试端点 `/api/test-advisor`（已确认main.py中的该端点是唯一残留的临时端点）
- 在后端main.py中添加了正式的 `/api/ai/advisor` 端点，带完整的认证、错误处理和日志
- 前端 `ai.js` 中的 `getAdvisor` 函数已从 `/api/test-advisor` 切换到正式的 `/api/ai/advisor`
- 顺带移除了前端3个多余的调试 `console.log` 和重复注释

**2. 改善流式响应性能**
- 原来：每次发送1个字符，asyncio.sleep(0.05) → 响应缓慢，系统调用极多
- 现在：每次发送3个字符，asyncio.sleep(0.02) → 保留打字机效果但性能提升约2.5x

**3. 修复文档统计路由冲突**
- 将 `/api/user/documents/stats` 路由移到 `/api/user/documents/{doc_id}` 之前
- FastAPI中静态路径必须先于动态路径注册，否则"stats"会被当作 doc_id 参数

**4. 删除会话临时文件路由（/api/session/*）**
- 移除了6个会话管理相关路由（创建会话、上传临时文件、获取会话上下文、获取活跃会话、获取临时文件内容、删除临时文件）
- 这些是实验性功能，尚未完善，不应暴露为正式API

---

### 📋 待决定/待讨论

**4. 系统知识库（QA.vue）的文档范围问题**
- 当前状态：QA页面（/api/lc/qa）仅能查询管理员上传的系统RAG文档
- 用户文档问答已有单独接口：`/api/user/qa/ask`，但前端QA.vue并没有使用它
- **分析意见**：
  - 现有设计是合理的：系统知识库 vs 个人知识库 分开
  - QA.vue定位为"系统知识库"查询，应该查管理员知识库（当前行为正确）
  - DocumentManager.vue里的QA默认基于用户自己的文档（使用/api/user/qa/ask）
  - 如果需要合并体验，可以在QA.vue添加一个切换器，让用户选择查询范围
  - **建议：保持现状，两者分开**，除非用户体验测试发现问题

